
<!DOCTYPE html>
<html>
<!-- Include Head -->
<%- include('includes/head', { title: 'Incluir Usuário' }) %>

	<body>
		<!-- Header -->
		<%- include('includes/header') %>
		<!-- /Header -->
        <div class="login-container">
			<div class="title-login">
			  <h1>LOGIN</h1>
			</div>

          <% if (typeof errors !== 'undefined') { %>
            <ul>
              <% for(let error in errors) { %>
                <li> <%= errors[error].msg %> </li>
              <% } %>
            </ul>
          <% } %>

          <div class='acess-login'>
			<form method="POST" action="/login/minha-conta" class="login-form">
			<input type="text" name='login' placeholder="E-mail" autocomplete="off">
			<input type="password" name='passaword' placeholder="Senha" autocomplete="off">
	  </div>
	  <div class="esqueci-senha">
		<a href="#" id="forgot-pass">Esquecer a senha?</a></div>
		<button type="submit">Entrar</button>
	</form>
	  <div class="register-container">
			<a href="/formulario">Criar Conta</a>
		</div>
	</div>

			<!-- footer -->
			<%- include('includes/footer') %>
			<!-- /footer -->
	</body>

</html>