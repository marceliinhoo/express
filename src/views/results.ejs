<!DOCTYPE html>
<html>
  <%- include('includes/head', { title: 'Início' }) %>

  <body>
        <%- include('includes/header') %>

      <div class="banner"></div>

      <div class="container products-wrapper">
        <div class="row">
          <div class="col-12">
            <h2 class="products-title">Resultados da pesquisa: <%= search %></h2>
          </div>
          <% if (products.length> 0) {
            products.forEach((product) => {
            let finalPrice = (product.price).toFixed(2)
          %>
          <div class="col-12 col-sm-6 col-lg-3">
            <section class="product-box">
              <a href="/product/detail/<%= `${product.id}/` %>">
                <figure class="product-box_image">
                  <img src="/images/Produtos/<%= product.image %>" alt="imagen do produto">
                </figure>
                <article class="product-box_data">
                  <h2>R$<%= toThousand(finalPrice) %>
                  </h2>
                  <p>
                    <%= product.name %>
                  </p>
                </article>
              </a>
            </section>
          </div>
          <% })} else { %>
            <div class="col-12">
              <h2 class="products-title" style="color: tomato;">Ei!! Infelizmente não foi possível encontrar esse produto. Veja se digitou corretamente o que procura<br>
              Aqui vai alguma sugestões de pesquisa:
              Beer; Double...</h2>
            </div>
          <% } %>
        </div>
      </div>

      <%- include('includes/footer') %>

  </body>

</html>